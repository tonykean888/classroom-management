# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type ClassroomType {
  classroomid: ID!
  classroom: String!
  academicyear: Float!
  homeroomTeacher: String!
  students: [StudentType!]
}

type GradelevelType {
  gradelevelid: Int!
  levelname: String!
}

type GenderType {
  genderid: Int!
  gendername: String!
}

type PrefixType {
  prefixid: Int!
  prefixname: String!
}

type StudentType {
  studentid: Int!
  firstname: String!
  lastname: String!
  birthdate: String!
  classrooms: [ClassroomType!]
  gradelevel: GradelevelType
  gender: GenderType
  prefix: PrefixType
}

type Query {
  hello: String!
  students(search: String, gradelevelid: Int, classroomid: Int): [StudentType!]!
  student(studentid: ID!): StudentType!
  studentsWithoutClassroom: [StudentType!]!
  classrooms: [ClassroomType!]!
  classroom(classroomid: ID!): ClassroomType!
  academicYears: [Float!]!
  searchClassrooms(classroomId: Int, classroomName: String, homeroomTeacherName: String): [ClassroomType!]!
  gradelevels: [GradelevelType!]!
  genders: [GenderType!]!
  prefixes: [PrefixType!]!
}

type Mutation {
  createStudent(input: CreateStudentInput!): StudentType!
  updateStudent(input: UpdateStudentInput!): StudentType!
  deleteStudent(studentid: ID!): Boolean!
  createClassroom(input: CreateClassroomInput!): ClassroomType!
  updateClassroom(input: UpdateClassroomInput!): ClassroomType!
  deleteClassroom(classroomid: Int!): ClassroomType!
  addStudentToClassroom(classroomid: Int!, studentid: Int!): ClassroomType!
  removeStudentFromClassroom(classroomid: Int!, studentid: Int!): ClassroomType!
}

input CreateStudentInput {
  prefixid: Int
  firstname: String!
  lastname: String!
  birthdate: DateTime!
  genderid: Int
  gradelevelid: Int
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

input UpdateStudentInput {
  studentid: Int!
  prefixid: Int
  firstname: String
  lastname: String
  birthdate: DateTime
  genderid: Int
  gradelevelid: Int
}

input CreateClassroomInput {
  academicyear: String!
  classroom: String!
  homeroomTeacher: String!
}

input UpdateClassroomInput {
  classroomid: Int!
  academicyear: String
  classroom: String
  homeroomTeacher: String
}